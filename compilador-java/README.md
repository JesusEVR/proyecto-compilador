# Ejecución de pruebas del compilador

Para poder correr las pruebas del analizador léxico y de los analizadores sintáticos, es necesario posicionarse a la altura donde se encuentra el archivo ***pom.xml*** (el mismo directorio donde se encuentra este archivo). 

Una vez ahí, se ejecuta el comando:

```
mvn clean test
```